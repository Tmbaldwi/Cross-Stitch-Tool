import { FileDragAndDrop } from './file-drag-and-drop';

describe('DragAndDrop', () => {
  it('should create an instance', () => {
    const directive = new FileDragAndDrop();
    expect(directive).toBeTruthy();
  });
});
